# Đồ thị vô hướng


::: tip CẤU TRÚC

```c
#include <stdio.h>
#define MAX_VERTEX 50 
typedef struct{
    int A[MAX_VERTEX][MAX_VERTEX];
    int n; //số đỉnh
    int m; //số cung
}Graph;

void makeNullList(Graph *G);                //* khởi tạo rỗng Graph
void init_Graph(Graph *G, int n);           //* tạo Graph với n đỉnh
void printGraph(Graph G);                   //* in ma trận Graph
int degree(Graph G, int x);                 //* tính bậc của đỉnh x
void DFS(Graph G, int x);                   //* duyệt theo chiều sâu Graph



void makeNullList(Graph *G){
    G->m = 0;
    G->n = 0;
}
void init_Graph(Graph *G, int n){
    for (int i = 1; i <= n; i++){
        for (int j = 1; j <= n; j++){
            G->A[i][j] = 0;
        }
    }
}
void printGraph(Graph G){
    for (int i = 1; i <= G.n; i++){
        for (int j = 1; j <= G.n; j++){
            printf("%d ",G.A[i][j]);
        }
        printf("\n");
    }
}
int degree(Graph G, int x){
    int count = 0;
    for (int i = 1; i <= G.n; i++){
        for (int j = 1; j <= G.n; j++){
            if(G.A[i][j] == 1){
                count++;
            }
        }
    }
    return count;
}
void DFS(Graph G, int x){
    
}
```
:::

name: Sync Algolia

on:
  push:
    branches:
      - 'main'

jobs:
  algolia:
    runs-on: ubuntu-latest
    name: Algolia Sync
    steps:
      - uses: actions/checkout@v2

      - uses: opstrace/algolia-docs-sync@v1.0.2
        with:
          algoliaId: '3CJDV2AFXL'
          algoliaKey: 'f7ab8030ae3f5983ef3e120c8a60b07'
          algoliaIndex: 'ctuvn'
